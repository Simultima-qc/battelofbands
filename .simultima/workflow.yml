schema_version: 1

# Non-secret operational profile. Repository reality and the latest central
# Playbook / AGENTS.md remain authoritative if this file becomes stale.
repository:
  default_branch: main

delivery:
  default_mode: DIRECT
  integration_branch: null
  production_branch: main
  production_deploy_from_implementation_merge: false

validation:
  commands:
    test: "cd server && npm test && cd ../client && npm test"
    lint: null
    build: "cd client && npm run build"

ci:
  workflow_name: "server-tests.yml + client-tests.yml"

deployment:
  provider: null
  preview_supported: false

database:
  # Current local/default provider. Public-alpha Postgres is code-ready but not provisioned yet.
  provider: sqlite
  migrations_path: server/db/migrations

environments:
  non_production: []
  production: null
